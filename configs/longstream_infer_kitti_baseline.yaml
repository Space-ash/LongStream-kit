# LongStream 推理配置：KITTI Odometry Baseline
#
# 数据来源：KITTI Odometry sequence 05（城市场景）
# 预处理：bash prepare_kitti_odometry.sh  （需提前准备 velodyne + calib_velo_to_cam.txt）
#
# 用途：基础验证，关闭 TTO 与筛帧，仅用基础重建流程，与 optimized 对照。
#
# Smoke test（前 100 帧快速验证）：
#   将 data.max_frames 设为 100

device: cuda

model:
  checkpoint: checkpoints/50_longstream.pt
  strict_load: false
  hf:
    repo_id: null
    filename: null
    revision: null
    local_dir: checkpoints
  longstream_cfg:
    img_size: 518
    patch_size: 14
    embed_dim: 1024
    window_size: 48
    use_role_embedding: false
    enable_scale_token: true
    disable_keyframe_distinction: true
    use_segment_mask: false
    enable_camera_head: false
    use_rel_pose_head: true
    freeze: none
    rel_pose_head_cfg:
      enabled: true
      keyframe_mode: fixed
      keyframe_stride: 8
      reference_source: pred
      detach_reference: false
      trunk_depth: 4
      pose_mode: SE3
      num_heads: 16
      mlp_ratio: 4
      init_values: 0.01
      trans_act: linear
      quat_act: linear
      use_pair_cross_attn: false
      xattn_temperature: 1.0
      use_precat: false
      use_kf_role_embed: false
      kf_role_embed_init_std: 0.02
      fl_act: relu
      use_global_scale: false
      reinit_camera_head: false

inference:
  mode: batch_refresh
  streaming_mode: causal
  window_size: 48
  keyframe_mode: fixed
  keyframe_stride: 8
  refresh: 4
  rel_pose_head_cfg:
    num_iterations: 4

data:
  format: generalizable
  data_roots_file: data_roots.txt
  img_path: prepared_inputs/kitti_odometry
  seq_list: ["05"]
  camera: "02"
  stride: 1
  max_frames: 200        # 改为 100 可进行 smoke test, null 则处理全部帧
  size: 518
  crop: false
  patch_size: 14
  gt_source: auto
  gt_poses_file: gt_poses.npy
  depth_gt_type: auto     # auto: 自动读取 depth_meta.json；无文件时当 dense

optimizations:
  filter:
    frame_filter_enabled: false
    confidence_filter_enabled: false
    blur_threshold: 100.0
    motion_threshold: 0.02
    confidence_threshold: 0.5
    save_frame_index_map: false
  correction:
    tto_enabled: false    # baseline: 关闭 TTO
  pose_post_correction:
    enabled: false
    method: segment_se3
    source: gps
    segment_size: 160
    overlap: 40
    min_points: 8
    blend: hann
    use_for_point_export: true
    save_raw_pose: true
    corrected_pose_name: abs_pose_corrected.txt

output:
  root: outputs/kitti_odometry_05/baseline
  save_videos: false
  save_images: true
  save_depth: true
  save_points: true
  save_frame_points: false
  save_point_head: false
  save_dpt_unproj: true
  enable_sky_mask: true
  save_sky_mask: false
  skyseg_path: model/skyseg.onnx
  skyseg_num_threads: 1
  save_depth_vis: false
  global_compact_pending_points: 800000
  max_full_pointcloud_points: 2000000
  max_frame_pointcloud_points: 200000
  rerun_current_points: null    # null = 不限制当前帧推送点数
  rerun_global_points: 30000
  rerun_use_blueprint: true
  rerun_view_coordinates: RIGHT_HAND_Y_DOWN
  live_global_checkpoint_interval: 0
  torch_num_threads: 2
  opencv_num_threads: 1

evaluation:
  align_scale: false
  depth_rel_delta_threshold: 1.25
  point_f1_threshold: 0.25
  point_eval_max_points: 100000
  point_eval_voxel_size: null
  point_eval_oversample_factor: 4
  # 稀疏 LiDAR GT 策略：仅计算 GT->pred recall，不计算对称 Chamfer/F1
  sparse_depth_pointcloud_policy: recall_only
  allow_sparse_chamfer_f1: false
  min_valid_depth_pixels: 1
